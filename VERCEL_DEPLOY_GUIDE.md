# Vercel 部署快速指南

## 🚀 方式一：GitHub 自动部署（推荐）

### 第一步：准备 GitHub 仓库

**如果还没有推送代码到 GitHub：**

```bash
# 1. 初始化 Git（如果还没有）
git init

# 2. 添加所有文件
git add .

# 3. 提交
git commit -m "准备部署到 Vercel"

# 4. 创建 GitHub 仓库后连接（替换下面的链接）
git remote add origin https://github.com/你的用户名/仓库名.git
git branch -M main
git push -u origin main
```

**如果已经有 GitHub 仓库：**
```bash
git add .
git commit -m "添加 Vercel 配置"
git push
```

---

### 第二步：部署到 Vercel

1. **访问 Vercel**  
   打开 https://vercel.com

2. **登录/注册**  
   点击 "Sign Up" → 选择 "Continue with GitHub"

3. **导入项目**  
   - 点击 "New Project"
   - 选择你的 GitHub 仓库（找到 `Webpage-template-for-BIM-application-of-Xiongan-Financial-Center`）
   - 点击 "Import"

4. **配置项目**  
   框架预设会自动识别为 **Vite**，无需修改：
   ```
   Build Command:    npm run build  ✓
   Output Directory: dist           ✓
   Install Command:  npm install    ✓
   ```

5. **部署**  
   点击 "Deploy" 按钮，等待 2-3 分钟

6. **完成**  
   看到 🎉 庆祝页面后，点击 "Visit" 查看网站

---

## 🚀 方式二：Vercel CLI 一键部署

### 第一步：安装 CLI

```bash
npm install -g vercel
```

### 第二步：登录并部署

```bash
# 在项目根目录执行
cd "f:\学习\AI\AI金融岛网页开发antigravity-Google AI STUDIO\Webpage-template-for-BIM-application-of-Xiongan-Financial-Center"

# 登录
vercel login

# 部署到生产环境
vercel --prod
```

按照终端提示操作：
- Setup and deploy? **Y**
- Which scope? 选择你的账号
- Link to existing project? **N**
- Project name? 直接回车（使用默认名称）
- In which directory? 直接回车（当前目录）
- Want to override settings? **N**

等待构建完成，终端会显示部署链接。

---

## ✅ 验证清单

部署完成后，请检查：

- [ ] 访问 Vercel 提供的链接（格式：`https://xxx.vercel.app`）
- [ ] 首页是否正常显示
- [ ] 点击导航菜单，测试所有页面跳转
- [ ] 测试后台登录（路径：`/admin`，密码：`Daiquan123`）
- [ ] 在后台编辑一些内容，确认能保存
- [ ] **分享链接给朋友**，让他们访问测试

---

## 🔧 常见问题

### 1. 构建失败

**错误信息**：`npm run build` 失败

**解决方法**：
```bash
# 在本地先测试构建
npm run build

# 如果成功，删除 node_modules 重新安装
rm -rf node_modules
npm install
git add .
git commit -m "修复依赖"
git push
```

### 2. 页面刷新后 404

**原因**：缺少 `vercel.json` 配置文件

**解决方法**：确认项目根目录存在 `vercel.json`（已为您创建）

### 3. 环境变量未生效

如果使用了 AI 功能：

1. 在 Vercel 项目页面点击 "Settings"
2. 左侧点击 "Environment Variables"
3. 添加：
   - **Name**: `VITE_AI_API_KEY`
   - **Value**: 你的 Gemini API Key
4. 保存后重新部署（点击 "Deployments" → 最新一次 → 右上角 "..." → "Redeploy"）

---

## 🎨 自定义域名（可选）

如果您有自己的域名（如 `abc.com`）：

1. 在 Vercel 项目中点击 "Settings" → "Domains"
2. 输入你的域名
3. 在域名服务商（如阿里云、Cloudflare）添加 DNS 记录：
   ```
   类型: CNAME
   名称: @
   值: cname.vercel-dns.com
   ```
4. 等待 10-30 分钟生效

---

## 📱 分享您的网站

部署成功后，您的网站链接格式为：

```
https://你的项目名.vercel.app
```

**复制此链接发送给任何人**，他们都可以访问！

即使您的电脑关机，网站也会 24/7 在线 🎉

---

## 🔄 后续更新网站

### 方式一（GitHub）：
```bash
# 修改代码后
git add .
git commit -m "更新内容"
git push
```
推送后 Vercel 会自动重新部署（约 1 分钟）

### 方式二（CLI）：
```bash
vercel --prod
```

---

**需要帮助？** 随时告诉我！
