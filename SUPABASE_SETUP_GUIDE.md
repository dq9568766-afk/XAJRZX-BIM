# Supabase 配置指南

本文档将指导您完成 Supabase 云数据库的配置，实现网站数据的云端存储与多人共享。

---

## 📋 第一步：注册 Supabase 账号

1. 访问 [https://supabase.com](https://supabase.com)
2. 点击 **"Start your project"** 或 **"Sign Up"**
3. 推荐使用 **GitHub 账号** 一键登录（最方便）

---

## 📋 第二步：创建新项目

1. 登录后点击 **"New Project"**
2. 填写项目信息：
   - **Name**: `financial-island-bim` (或任意名称)
   - **Database Password**: 设置一个强密码（务必记住，后续可能用到）
   - **Region**: 选择 **Northeast Asia (Tokyo)** 或 **Southeast Asia (Singapore)**（离中国最近）
3. 点击 **"Create new project"**，等待 2-3 分钟创建完成

---

## 📋 第三步：获取 API 密钥

项目创建完成后：

1. 点击左侧菜单 **"Project Settings"** (齿轮图标)
2. 选择 **"API"** 选项卡
3. 复制以下两个值：
   - **Project URL**: 类似 `https://xxxxxxxx.supabase.co`
   - **anon public**: 类似 `eyJhbGciOiJIUzI1...` (很长的一串)

---

## 📋 第四步：创建数据库表

1. 点击左侧菜单 **"SQL Editor"** (数据库图标)
2. 点击 **"New query"**
3. 复制项目根目录的 `supabase-init.sql` 文件内容，粘贴到编辑器
4. 点击 **"Run"** 执行
5. 看到 **"Success. No rows returned"** 表示创建成功

---

## 📋 第五步：创建存储桶 (Storage Buckets)

**✨ 好消息：上一步的 SQL 脚本已经自动为您创建了所有存储桶和访问策略！**

您只需要在左侧菜单点击 **"Storage"**，确认可以看到以下三个存储桶：

| 名称 | Public | 用途 |
|------|--------|------|
| `images` | ✅ 开启 | 存放图片 |
| `videos` | ✅ 开启 | 存放视频 |
| `documents` | ✅ 开启 | 存放文档 |

如果看到这些，说明配置成功，**无需手动操作**。

---

## 📋 第六步：配置环境变量

在项目根目录找到 `.env.local` 文件（如果没有就创建一个），添加：

```env
# Supabase 配置
VITE_SUPABASE_URL=https://你的项目ID.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon公钥

# AI 配置 (可选)
VITE_GEMINI_API_KEY=你的Gemini API Key
```

⚠️ **重要**：`.env.local` 文件**不要**提交到 Git！它已经被 `.gitignore` 排除。

---

## 📋 第七步：验证配置

1. 重启开发服务器：
   ```bash
   npm run dev
   ```

2. 打开浏览器控制台 (F12 → Console)，应该看到：
   ```
   ✅ Supabase 连接成功
   ```

3. 如果看到 `⚠️ Supabase 未配置`，请检查 `.env.local` 是否正确

---

## 📋 第八步：迁移现有数据 (可选)

如果之前在 localStorage 中已有数据，需要迁移到 Supabase：

1. 打开浏览器控制台
2. 运行以下命令导出现有数据：
   ```javascript
   const data = {
     projectInfo: JSON.parse(localStorage.getItem('projectInfo') || '{}'),
     highlights: JSON.parse(localStorage.getItem('highlights') || '[]'),
     achievements: JSON.parse(localStorage.getItem('achievements') || '[]'),
     teamMembers: JSON.parse(localStorage.getItem('teamMembers') || '[]'),
     // ... 其他数据
   };
   console.log(JSON.stringify(data, null, 2));
   ```
3. 复制输出的 JSON
4. 在 Supabase 后台 → Table Editor → 手动导入数据

---

## 🎉 配置完成！

现在您的网站数据将存储在 Supabase 云端：
- ✅ 任何人访问网站都能看到同样的内容
- ✅ 上传的视频/文档存储在云端，不受本地限制
- ✅ 数据安全持久，不会因清除浏览器而丢失

---

## 📞 常见问题

### Q: 免费版有什么限制？
**A**: Supabase 免费版包含：
- 500MB 数据库存储
- 1GB 文件存储
- 2GB 带宽/月
- 对于一般的 BIM 展示网站完全够用！

### Q: 如何查看已上传的文件？
**A**: 在 Supabase 后台 → Storage → 点击对应存储桶即可浏览

### Q: 如何备份数据？
**A**: 在 Supabase 后台 → Project Settings → Database → 点击 "Download backup"

---

## 🔗 相关链接

- [Supabase 官方文档](https://supabase.com/docs)
- [Supabase 存储文档](https://supabase.com/docs/guides/storage)
- [Supabase 定价](https://supabase.com/pricing)
